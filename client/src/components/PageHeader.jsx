export default function PageHeader({ eyebrow = 'Saileela Mandal Palkhi', title, children }) { return <section className="page-header"><div className="shell"><span className="page-header-mark" aria-hidden="true">✦</span><p className="eyebrow">{eyebrow}</p><h1>{title}</h1>{children && <p className="page-lede">{children}</p>}<div className="page-header-rule" aria-hidden="true"/></div></section>; }
