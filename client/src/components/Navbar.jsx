import { Link, NavLink } from 'react-router-dom';
import { useLanguage } from '../context/LanguageContext';
import { useState } from 'react';
export default function Navbar() { const { language, setLanguage, t } = useLanguage(); const [open, setOpen] = useState(false); const links = [['/', 'home'], ['/about', 'about'], ['/schedule', 'palkhi'], ['/gallery', 'gallery'], ['/seva', 'seva'], ['/register', 'register'], ['/contact', 'contact']]; return <header className="navbar"><div className="shell nav-inner"><Link className="brand" to="/"><img src="/logo.png" alt="साईलीला मंडळ पालखी logo"/><span><strong>साईलीला मंडळ</strong><small>Saileela Mandal Palkhi</small></span></Link><button className="menu-button" onClick={() => setOpen(!open)} aria-label="Toggle navigation">☰</button><nav className={open ? 'nav-links open' : 'nav-links'}>{links.map(([path, key]) => <NavLink key={path} to={path} onClick={() => setOpen(false)}>{t(key)}</NavLink>)}<NavLink className="nav-cta" to="/donate">{t('donate')}</NavLink><NavLink to="/admin/login">{t('admin')}</NavLink><div className="language-toggle"><button className={language === 'mr' ? 'selected' : ''} onClick={() => setLanguage('mr')}>मराठी</button><button className={language === 'en' ? 'selected' : ''} onClick={() => setLanguage('en')}>EN</button></div></nav></div></header>; }
