import { Link, NavLink } from 'react-router-dom';
import { useLanguage } from '../context/LanguageContext';
import { useEffect, useState } from 'react';
export default function Navbar() { const { language, setLanguage, t } = useLanguage(); const [open, setOpen] = useState(false); const links = [['/', 'home'], ['/about', 'about'], ['/schedule', 'palkhi'], ['/gallery', 'gallery'], ['/seva', 'seva'], ['/register', 'register'], ['/contact', 'contact']]; useEffect(() => { const close = (event) => { if (event.key === 'Escape') setOpen(false); }; window.addEventListener('keydown', close); return () => window.removeEventListener('keydown', close); }, []); return <header className="navbar"><div className="shell nav-inner"><Link className="brand" to="/" onClick={() => setOpen(false)}><img src="/logo.png" alt="साईलीला मंडळ पालखी logo"/><span><strong>साईलीला मंडळ</strong><small>Saileela Mandal Palkhi</small></span></Link><button className="menu-button" onClick={() => setOpen(!open)} aria-label="Toggle navigation" aria-expanded={open}>☰</button><nav className={open ? 'nav-links open' : 'nav-links'} aria-label="Primary navigation">{links.map(([path, key]) => <NavLink key={path} to={path} onClick={() => setOpen(false)}>{t(key)}</NavLink>)}<NavLink to="/faq" onClick={() => setOpen(false)}>FAQ</NavLink><NavLink className="nav-cta" to="/donate" onClick={() => setOpen(false)}>{t('donate')}</NavLink><NavLink to="/admin/login" onClick={() => setOpen(false)}>{t('admin')}</NavLink><div className="language-toggle" aria-label="Language selection"><button type="button" className={language === 'mr' ? 'selected' : ''} onClick={() => setLanguage('mr')}>मराठी</button><button type="button" className={language === 'en' ? 'selected' : ''} onClick={() => setLanguage('en')}>EN</button></div></nav></div></header>; }
